<%= form_with(model: menu_items_order, local: true) do |form| %>
  <% if menu_items_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(menu_items_order.errors.count, "error") %> prohibited this menu_items_order from being saved:</h2>

      <ul>
      <% menu_items_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :menu_item_id %>
    <%= form.number_field :menu_item_id, id: :menu_items_order_menu_item_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.number_field :order_id, id: :menu_items_order_order_id %>
  </div>
  <div class="field">
    <%= form.label :credit %>
    <%= form.number_field :credit, id: :menu_items_order_credit %>
  </div>

  <div class="actions">
  	<p>If you mess up ordering you gotta start over!</p>
    <%= form.submit %>
  </div>
<% end %>
